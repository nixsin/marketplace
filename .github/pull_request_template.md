## Summary
<!-- What changed, as 1-3 bullet points. -->

## Why
<!-- The problem or goal this solves. Link an issue if one exists. -->

## Verification
<!-- How you confirmed this works: commands run, screenshots, what you
     checked in a browser. "Trust me" isn't verification. -->

### Tests & coverage
<!-- "Before" = main at the commit this branch forked from; "After" = this
     branch, from CI's actual job output (or a local run of the same
     command). Leave a row blank with a one-line reason if it doesn't apply
     (e.g. a docs-only change) rather than deleting it. -->

| | Before | After |
|---|---|---|
| Unit tests (pass/fail counts) | | |
| Integration/e2e tests (pass/fail counts) | | |
| Coverage — statements / branches / functions / lines | | |

### UX / visual changes
<!-- Only for changes that could plausibly affect what a user sees or
     interacts with — delete this subsection otherwise. Drag in a
     screenshot of the affected page/component from before this change
     and from after, plus a one-line note on what changed and why. -->

**Before:**


**After:**


**What changed:**


### Performance
<!-- Only for changes that could plausibly affect load time, bundle size,
     or response time — delete this subsection otherwise. Use real numbers
     (Navigation Timing API, bundle size output, response times), not
     guesses. If the scenario this change targets isn't reproducible on
     demand (e.g. a cold start that only happens after real idle time),
     say so explicitly rather than omitting the row or leaving it blank
     unexplained. -->

| | Before | After |
|---|---|---|
| | | |

## Reviewer notes
<!-- Anything a reviewer should know that isn't obvious from the diff:
     tradeoffs, deliberately out-of-scope items, follow-ups. Delete this
     section if there's nothing to add. -->
