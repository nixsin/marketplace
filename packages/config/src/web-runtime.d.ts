/**
 * Reading either of these throws when the variable is unset. There is no
 * default: a localhost fallback here publishes localhost links to real
 * visitors, so absence has to be loud.
 */
export declare const API_URL: string;
export declare const SITE_URL: string;
